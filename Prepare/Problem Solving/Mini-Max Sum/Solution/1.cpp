#include <iostream>
#include <vector>

using namespace std;

void miniMaxSum(vector<int> arr) {
    int n = arr.size();
    long long minSum = 0, maxSum = 0;
    for (int i = 0; i < n; i++) {
        for (int j = i + 1; j < n; j++) {

        }
    }
}