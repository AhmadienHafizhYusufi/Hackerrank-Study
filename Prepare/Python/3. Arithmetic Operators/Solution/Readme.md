We can do the arithmetic operation inside the `print(...)` directly:

1. Sum of the two numbers.

```python
print(a + b)
```

2. Difference of the two numbers.

```py
print(a - b)
```

3. Product of the two numbers

```py
print(a * b)
```
